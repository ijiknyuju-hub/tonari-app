1. 最終成果物は1料理1レコードが適切です。introの反復調整も同じレコードの2フィールドだけを再生成すれば足ります。ただし、凍結済みの特徴量はCodexに再生成させず、既存データを正とした決定的な結合にすべきです。

2. JSON中間形式が適切です。Codex出力は `dish-v4` と同形のフラットな配列に `intro_line1` と `intro_line2` を加えた最小形にします。`generated_at` やハッシュはモデル出力に含めず、必要なら後段で決定的に付与・検証します。`data/v3.ts` は既存16消費側とスコープ外の移行を巻き込むため不適切です。

3. 約50件ごとの複数実行が適切です。17件のエントリー料理は専用の1バッチ、残り265件は約44〜45件の6バッチ、計7実行を推奨します。この規模ではコンテキスト上限よりも、長大な構造化出力の欠落・形式崩れ・文体劣化のリスクが支配的です。プロンプトと例文を固定し、各バッチの期待料理名を機械照合すれば一貫性は保てます。

4. 最大の追加リスクは、凍結・検証済み特徴量をCodexが微妙に改変することです。JSONは構文・必須項目・282件の集合一致を検査できますが、意味的な改変やintroの類似文を防ぐものではありません。全料理名の集合一致、重複名・欠落・余剰の検査、元特徴量との完全一致、introの文字列類似度検査を別途必須にしてください。単一レコード化は欠落と結合漏れを見つけやすくしますが、類似intro自体には効きません。